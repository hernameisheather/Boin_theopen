# 로컬 실행시 .env 파일로 복사해서 사용 (cp .env.example .env)
# Render 배포시에는 Environment 탭에서 같은 변수를 등록

# 세션 암호화에 사용. 영문/숫자 32자 이상 무작위로 설정 권장.
APP_SECRET=please-change-this-to-a-long-random-string

# 관리자 페이지 비밀번호 (선생님만 아는 값)
ADMIN_PASSWORD=teacher1234
